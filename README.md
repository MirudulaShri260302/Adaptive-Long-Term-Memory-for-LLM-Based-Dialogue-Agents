# Adaptive-Long-Term-Memory-for-LLM-Based-Dialogue-Agents
This repository implements an adaptive long-term memory for LLM agents. It uses a fixed token budget to decide what to KEEP verbatim, STORE in structured slots, or DROP. By replacing static sliding windows with human-like selective retention, it improves consistency and constraint adherence in long dialogues.
